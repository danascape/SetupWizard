<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: 2016 The CyanogenMod Project
     SPDX-FileCopyrightText: The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<com.google.android.setupdesign.GlifLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/setup_wizard_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Takes the place the floating back button occupies on every other screen. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/emerg_dialer"
            style="?attr/materialIconButtonFilledStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/welcome_action_button_margin_start"
            android:layout_marginTop="@dimen/welcome_action_button_margin_top"
            android:contentDescription="@string/emergency_call"
            android:minWidth="@dimen/welcome_action_button_size"
            android:minHeight="@dimen/welcome_action_button_size"
            app:backgroundTint="?attr/colorSurfaceContainerHigh"
            app:icon="@drawable/ic_emergency_dial"
            app:iconTint="?attr/colorOnSurface" />

        <Space
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="2" />

        <ImageView
            android:id="@+id/brand_logo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:src="@drawable/logo" />

        <Space
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="0.5" />

        <LinearLayout
            style="@style/SudContentFrame"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="@dimen/welcome_content_container_padding_start"
            android:paddingEnd="@dimen/welcome_content_container_padding_end">

            <TextView
                android:id="@+id/welcome_title"
                style="@style/WelcomeTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hyphenationFrequency="none"
                android:paddingBottom="@dimen/welcome_content_container_padding_bottom" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/launch_accessibility"
                style="?attr/materialIconButtonFilledStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="start|center_vertical"
                android:paddingStart="@dimen/welcome_label_button_padding_horizontal"
                android:paddingEnd="@dimen/welcome_label_button_padding_horizontal"
                android:paddingTop="@dimen/welcome_label_button_padding_vertical"
                android:paddingBottom="@dimen/welcome_label_button_padding_vertical"
                android:text="@string/accessibility_settings"
                android:textSize="@dimen/welcome_accessibility_text_size"
                app:backgroundTint="?attr/colorSurfaceContainerHigh"
                app:icon="@drawable/ic_eye"
                app:iconGravity="textStart"
                app:iconPadding="@dimen/welcome_label_button_icon_padding"
                app:iconTint="?attr/colorOnSurface" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingStart="@dimen/welcome_content_container_padding_start"
            android:paddingEnd="@dimen/welcome_content_container_padding_end"
            android:paddingTop="@dimen/welcome_footer_padding_top"
            android:paddingBottom="@dimen/welcome_content_container_padding_bottom">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/skip"
                style="@style/SucGlifMaterialButton.Secondary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginEnd="@dimen/welcome_footer_padding_top"
                android:text="@string/skip"
                android:visibility="gone" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/start"
                style="?attr/materialIconButtonFilledStyle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:contentDescription="@string/start"
                android:minHeight="@dimen/welcome_start_button_min_height"
                app:icon="@drawable/ic_arrow_forward"
                app:iconGravity="textStart"
                app:iconSize="@dimen/welcome_start_button_icon_size" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.setupdesign.GlifLayout>
