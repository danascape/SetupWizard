<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="?attr/sudItemContainerStyle"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:baselineAligned="false"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:baselineAligned="false"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- Unused, but Item binds it unconditionally. -->
        <FrameLayout
            android:id="@id/sud_items_icon_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone">

            <ImageView
                android:id="@id/sud_items_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <com.google.android.setupdesign.view.RichTextView
                android:id="@id/sud_items_title"
                style="?attr/sudItemTitleStyle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="start"
                android:textAlignment="viewStart" />

            <com.google.android.setupdesign.view.RichTextView
                android:id="@id/sud_items_summary"
                style="?attr/sudItemSummaryStyle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="start"
                android:textAlignment="viewStart"
                android:visibility="gone" />

        </LinearLayout>

        <ImageView
            android:id="@+id/device_info_expand"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:contentDescription="@null" />

    </LinearLayout>

    <ImageView
        android:id="@+id/device_info_barcode"
        android:layout_width="match_parent"
        android:layout_height="@dimen/device_info_barcode_height"
        android:layout_marginTop="@dimen/device_info_barcode_margin_top"
        android:contentDescription="@null"
        android:scaleType="fitXY"
        android:visibility="gone" />

</LinearLayout>
